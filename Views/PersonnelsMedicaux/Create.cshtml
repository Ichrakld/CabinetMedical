@model GestionCabinetMedical.Models.PersonnelMedical

@{
    ViewData["Title"] = "Ajouter Personnel";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container mt-4">
    <div class="page-header mb-4">
        <h1><i class="fas fa-user-plus text-success"></i> Ajouter un Membre</h1>
    </div>

    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card shadow-sm">
                <div class="card-header bg-light">
                    <h5 class="mb-0 text-secondary"><i class="fas fa-id-card"></i> Nouveau collaborateur</h5>
                </div>
                <div class="card-body">
                    <form asp-action="Create">
                        <div asp-validation-summary="ModelOnly" class="alert alert-danger" role="alert"></div>

                        <div class="mb-3">
                            <label asp-for="Nom" class="form-label fw-bold">
                                <i class="fas fa-user text-primary"></i> Nom Complet
                            </label>
                            <input asp-for="Nom" class="form-control" placeholder="Ex: Jean Dupont" />
                            <span asp-validation-for="Nom" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="Fonction" class="form-label fw-bold">
                                <i class="fas fa-briefcase text-success"></i> Fonction / Poste
                            </label>
                            <input asp-for="Fonction" class="form-control" placeholder="Ex: Infirmier, Secrétaire..." />
                            <span asp-validation-for="Fonction" class="text-danger"></span>
                        </div>

                        <div class="d-grid gap-2 d-md-flex justify-content-md-end mt-4">
                            <a asp-action="Index" class="btn btn-secondary me-md-2"><i class="fas fa-times"></i> Annuler</a>
                            <button type="submit" class="btn btn-success"><i class="fas fa-save"></i> Enregistrer</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }

}